# Project notes

The front-end app lives in [`web/`](web). Run all npm commands from that directory.

```bash
cd web
npm install
npm run dev
npm run build
```

Do not commit `.env.local` or local Blender backup files. The public web model is `web/public/models/me.glb`.
